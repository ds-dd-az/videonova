export const rootSelector = (state) => state

export const selectVideoUrl = (state) => state.videos[0].videoUrl

export const selectVideoName = (state) => state.videos[0].videoUrl

export const selectDescription = (state) => state.videos[0].description
